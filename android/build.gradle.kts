// Top-level build file (Kotlin DSL)
// En proyectos Flutter modernos todo se configura en settings.gradle.kts
// Este archivo se deja mínimo para evitar conflictos con AGP 9+ y el Flutter Gradle plugin
